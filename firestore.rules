rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /brackets/{bracketId} {
      allow read: if true;
      allow write: if request.resource.data.pin == "__SHARED_PIN__";
    }
  }
}
